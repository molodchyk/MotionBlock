# Store Assets

Chrome Web Store submission materials live here.

- `store-listing/en.txt`: English detailed description for the Store listing.
- `promo/`: Chrome Web Store promotional tiles.
- `screenshots/`: Store screenshots. Keep final screenshots here instead of mixing them with runtime extension assets.

Runtime extension icons remain in `assets/icons/` because `manifest.json` references them.
