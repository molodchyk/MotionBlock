# Privacy

MotionBlock stores extension settings locally through Chrome extension storage.

The extension does not collect, sell, transmit, or analyze personal data. It does not use analytics, remote configuration, tracking pixels, or external services.

MotionBlock reads page media elements only to apply the user's selected blocking rules. Per-site preferences are stored as hostnames so the extension can apply user-selected rules on matching websites.
